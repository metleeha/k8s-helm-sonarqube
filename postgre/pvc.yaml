apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: hdlee-postgre-pvc
  labels:
    type: postgre
spec:
  storageClassName: postgre
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
