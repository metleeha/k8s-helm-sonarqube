apiVersion: v1
kind: PersistentVolume
metadata:
  name: hdlee-postgre-pv
  labels:
    type: postgre
spec:
  storageClassName: postgre
  accessModes:
    - ReadWriteOnce
  capacity:
    storage: 10Gi
  hostPath:
    path: "/mnt/postgre"

