apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: lab99-sonarqube-pvc
  labels:
    type: sonarqube
spec:
  storageClassName: sonarqube
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 8Gi
